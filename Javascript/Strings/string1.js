// String methods that doesn't take an argument.

// Convert message into lower case and trim the extra spaces

const message = "    TASTE THE RAINBOW!  ";

let whisper = message.trim().toLowerCase();

